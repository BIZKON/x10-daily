# Handoff · Session 30 — Спека 2 доделана: ИИ-обложки статей (Task 2–7)

**Дата:** 2026-08-03 … 05 · **Прод после сессии:** `d169605` (Спека 2 + фикс конвейера)
**Прод на старте сессии:** `987d0c5` · `app.pro-agent-ai.ru` жив, контейнеры Up 7 дней, автономный контур постит.
**Предыдущий handoff:** `docs/handoffs/handoff-session-29.md`.

> **Первое действие новой сессии:** сверить git вживую (`git log --oneline origin/main..HEAD`)
> и состояние прода (`ssh root@37.77.105.82`, `git log -1`), не доверяя цифрам ниже.

---

## 0. TL;DR

- **Спека 2 (ИИ-обложки) реализована целиком — Task 2–7** и **задеплоена**.
- 🔴 **Отдельно вскрыт и починен инцидент**: конвейер не писал статей с 31.07
  (см. §4а) — устаревший `max_tokens` вместо `max_completion_tokens`.
- **ИИ-обложки ВКЛЮЧЕНЫ на проде** (`COVERS_PUBLIC_BASE_URL` задан 05.08,
  бэкап env — `/opt/x10-daily/.env.production.bak.s30`).
- **Живая проверка шлюза выполнена:** модель `gemini/gemini-3.1-flash-image-preview`
  доступна (101 модель в каталоге), генерация новым каноничным промптом отработала
  за 7 с → JPEG 448 КБ, 1376×768. `message.content === null` подтверждён повторно.
- **Качество картинки — в каноне** `packages/voice/visual.md`: документальный кадр,
  приглушённый steel, красный дозированно, без текста/неона/роскоши/лиц/логотипов.
- **Схема прода:** миграция `0014` ещё НЕ применена (приедет с `./deploy.sh`).
  Обложек в БД **0 из 2707 статей** → правки ленты для существующего контента no-op.


## 0а. ✅ Live-проверка после деплоя (05.08.2026, 09:18–09:22 UTC)

Прод `d169605`, контейнеры пересозданы, обложки включены.

**Фикс конвейера подтверждён вживую** — первые успешные прогоны с 31.07:

```
09:18:44  draft   succeeded  out=47076
09:19:09  visual  succeeded  out=1385
09:19:39  draft   succeeded  out=47345
09:19:56  visual  succeeded  out=722
```

47 076 и 47 345 выходных токенов против прежней отсечки 8192 — `max_completion_tokens`
снял ограничение. Ошибок в прогонах нет.

**Спека 2 отработала сквозняком с первой же статьи:**

| Проверка | Результат |
|---|---|
| Статьи созданы | 2 (первые с 31.07) |
| `visual_status` | обе `pending_review` |
| 🔴 `approved` в БД | **0** — ИИ не опубликовал сам |
| Файлы на диске | 392 КБ и 581 КБ, 1376×768 |
| Раздача Caddy | HTTP 200, ~1 с |
| Очередь канала | 2 непостнутые tg-строки (уйдут в слот) |

Качество обложек — в каноне: приглушённый фон, steel, тонкие золотые линии как
хром, красный дозированной точкой, текста в кадре нет, одна метафора на кадр.


## 0б. Спека 3 — единый стиль обложек «цельный постер» (05.08.2026)

Владелец попросил единый стиль в духе обложек YouTube — с логотипом и рекламной
строкой. Выбран стиль **А «нижняя плашка»** из четырёх показанных раскладок.

🔴 **Ключевой поворот: исходная посылка была неверной.** Канон `visual.md`
запрещал модели рисовать текст («коверкает кириллицу»), и первая реализация шла
через наложение текста поверх картинки (`/api/cover` на `next/og`). Владелец
возразил, что Nano Banana 2 хорошо рисует по-русски. **Замер подтвердил его:**
кириллица чистая, включая брендовый знак и кнопку. Слой наложения удалён,
осталась одна цельная генерация.

**Что даёт цельная генерация:** кадр не распадается на «картинку и наклейку»,
текст живёт внутри композиции. Прежний вариант давал «картинку в картинке»:
градиент съедал сцену, а панель выглядела наклеенной.

### Выверенные замерами формулировки промпта

| Правило | Почему |
|---|---|
| Надписи **без кавычек** вокруг них | Модель рисует сами ёлочки |
| **Никаких hex-кодов** | В серии отрисовала «#D4A24C» как текст в кадре |
| Знак — геометрией, **без слова «логотип»** | Формулировки «ЛОГОТИП»/«фирменный знак» чаще уходят под фильтр |
| Слово «логотипы» убрать и из запретов | Путает модель, когда знак в кадре нужен |

### Надёжность (замерено, не оценка)

Цельный постер проходит **21/28 ≈ 75%**; сцена без надписей — 11/11. Отказ:
`finish_reason=content_filter`, ноль картинок, HTTP 200. Идёт **волнами во
времени** — мгновенный повтор бесполезен, через паузу проходит. Отсюда
`ImageContentFilterError` отдельным классом и 3 попытки с паузой 20 с; сетевые
ошибки здесь НЕ ретраятся (это работа Inngest). Все попытки провалены →
`visual_status` не меняется → пост уходит текстом.

⚠️ **Методическая грабля (стоила часа).** Сравнивать варианты промпта БЛОКАМИ
нельзя: отказы волновые, и блочное сравнение даёт ложные выводы. Я так
«локализовал» несуществующий триггер (0/4 против 4/4), а чередующийся тест
показал 14/14 отказов у ОБОИХ вариантов. Сравнивать только чередованием по одному.

### Live-проверка на проде

Прод `193ac7b`. Перегенерированы 6 обложек — **все 6 с первой попытки, отказов
ноль**. `VisualAgent` сам сжимает длинный `tease` в заголовок кадра: «Сайты за 20
минут: почему история 17-летней фрилансерки — это не бизнес-модель, а ловушка» →
«Ловушка быстрых сайтов». CTA чередуется по рубрике корректно.

## 1. Что сделано

| Task | Что | Файлы |
|---|---|---|
| 2 | VisualAgent — промпт иллюстрации | `packages/agents/src/agents/visual.ts` |
| 3 | Клиент image-модели + хранилище на диске | `apps/workers/pipeline/src/lib/{gemini-image,cover-storage}.ts` |
| 4 | Inngest `generate-cover` + событие | `.../inngest/functions/generate-cover.ts`, `src/events.ts` |
| 5 | Ревью в админке | `apps/api/src/routes/admin-visual.ts`, `apps/admin/src/app/visuals/*` |
| 6 | Фото-пост в канал + обложка в ленте | `.../lib/caption.ts`, `drain-post-slots.ts`, `post-channel.ts`, `apps/api/src/routes/feed.ts` |
| 7 | Раздача Caddy + версия в URL | `caddy/Caddyfile.prod`, `.../lib/cover-storage.ts` |

**Тесты:** pipeline 196/196, api 34/34, agents 60/60. Typecheck чист во всех пакетах.
Сборки miniapp и admin проходят, PPR цел.

## 2. Отклонения от плана (каждое обосновано в коде)

1. **Промпт по канону `visual.md`, а не по черновику плана.** Черновик просил
   `subtle glow` и тёмный charcoal-фон — канон это прямо запрещает («не использует
   неон, киберпанк, AI-свечение»; палитра приглушённая, золото только как тонкий
   хром). Порядок блоков канонический: STYLE → PILLAR → SUBJECT → NEGATIVE → TECH.
   NEGATIVE дописывается ПОСЛЕ сцены — модель-крафтер не может протащить в кадр
   текст, лицо или логотип.
2. **Генерация и запись — ОДИН Inngest-шаг** (в плане были раздельные `generate` и
   `store`). Inngest сериализует результаты `step.run` в JSON: ~450–800 КБ байтов
   картинки через границу шага распухли бы в `{"0":255,…}` и упёрлись в лимит вывода.
   Наружу отдаётся только публичный URL. Плата — ретрай перегенерирует картинку
   (`retries:1` → максимум два вызова модели).
3. **URL обложки несёт версию содержимого** `?v=<хеш байтов>`. Имя файла
   детерминировано по `articleId`, перегенерация ПЕРЕЗАПИСЫВАЕТ тот же путь — с
   `immutable`+1год из плана новая картинка навсегда осталась бы невидимой.
4. **Экран админки — `/visuals`**, не `(dash)/visuals`: такой группы маршрутов в
   админке нет (маршруты плоские).
5. **Сверх плана — гейт `visual_status` в API ленты.** Без него отклонённая
   редактором («без картинки») или ещё не отсмотренная ИИ-обложка всё равно
   показывалась бы в ленте Mini App — дыра в HumanGate. Теперь лента отдаёт
   `coverImageUrl` только при статусе `approved` или `none` (`none` = ручная/легаси
   обложка, к ИИ-генерации отношения не имеет).

## 3. Как это работает (контур целиком)

```
draft-article ──persist──▶ событие article/cover.requested
                                    │
                            generate-cover (Inngest)
                    load-article → visual-prompt (VisualAgent, DeepSeek РФ)
                    → generate-and-store (Nano Banana 2 → JPEG → том covers)
                    → visual_status = 'pending_review'        🔴 НИКОГДА не approved
                                    │
                        админка /visuals — решение редактора
              одобрить → approved │ без картинки → rejected │ перегенерить → force
                                    │
   drain-post-slots (слоты 4/день): approved + есть URL → sendPhoto + подпись ≤1024
                                     иначе               → текстовый пост, как раньше
```

**Фолбэк железный:** любой сбой (шлюз, диск, агент) → `visual_status` не становится
`pending_review` → канал постит текстом, лента показывает `BrandedCover`.

**Выключатель:** пустой `COVERS_PUBLIC_BASE_URL` → `generate-cover` скипается с
причиной `covers-disabled`, поведение прода ровно как до сессии.

## 3а. Адверс-ревью перед деплоем (Workflow `wr7yj18a6`)

6 линз → 39 агентов, каждая находка проходила отдельного агента-опровергателя.
**32 вердикта, 15 подтверждено, 13 записей схлопнулись в 7 уникальных причин.**

**Два блокера — оба ПОЧИНЕНЫ до деплоя:**

- 🔴 **Б1 (живой канал).** `sendPhoto` с HTTP 400 по САМОЙ картинке (Telegram не
  смог скачать URL / `IMAGE_PROCESS_FAILED`) не деградировал в текст: фолбэк слал
  ту же картинку, финальный вызов не был обёрнут в try/catch, бросок уходил из
  шага. Слот терялся целиком, `recordChannelFailure` не вызывался (нет ни
  `attempts`, ни `last_error`), а выборка FIFO брала ту же строку каждым
  следующим слотом в пределах `STALE_HOURS=24` — **до четырёх молчащих слотов
  подряд**. Это была регрессия относительно текущего прода, где фото-ветка была
  мёртвой. Фикс: цепочка деградации фото-HTML → фото-plain → rich-HTML текстом →
  плоский текст, и текстовый пост строится ВСЕГДА, а не только когда обложки нет.
- 🔴 **Б2 (HumanGate).** Гейт `visual_status` я поставил в ленте, но **не в
  читалке** — а именно туда ведёт deep-link из каждого поста канала. Неодобренная
  обложка была бы видна каждому читателю hero-баннером, а отклонённая («без
  картинки») — навсегда, потому что `reject` не обнуляет `cover_image_url`.
  Фикс: `gateCoverByVisualStatus` в `apps/api/src/routes/articles.ts`, покрыт
  тестами на все пять статусов.

**Два MED тоже закрыты сразу** (лежат в пути живого канала):
`«Перегенерировать»` снимает одобрение (`generating`) ДО генерации — иначе
перезапись файла по тому же пути обесценивала одобренные байты, и слот мог
отправить в канал непросмотренную картинку; `Cache-Control: immutable` больше не
вешается на 404 (матчер `@exists`) — иначе клиент закэшировал бы «нет картинки»
на год в окно первого включения.

**Осталось незакрытым (не блокер):** при броске шага отправки
`recordChannelFailure` не вызывается → в `channels` нет диагностического следа
(`attempts`/`last_error`). После Б1 бросок стал маловероятным, но след полезен.

## 4. Грабли сессии (переиспользуемо)

- **Inngest json-ит результаты `step.run`** — бинарные данные через границу шага возить нельзя.
- **Лимит caption Telegram (1024) считается ПОСЛЕ разбора сущностей**: теги не входят,
  `&amp;` = один символ. Резать надо по видимой длине, иначе либо теряешь место, либо ловишь 400.
- **`escapeTelegramHtml` не экранирует кавычку** — для значения атрибута `href` этого мало,
  URL с `"` разорвал бы атрибут (в `caption.ts` добавлено отдельное экранирование).
- **PPR в админке:** страница, чей data-fetch выходит раньше обращения к `cookies()`
  (на билде `X10_API_BASE_URL` не задан), собирается как `○ Static` и запекает
  «Данные недоступны» навсегда. Диагностика: `postponed` в `.next/server/app/<route>.meta`.
  Лечится `connection()` внутри Suspense-компонента.
- **`handle`-блоки Caddyfile срабатывают в порядке записи** — `handle_path /covers/*`
  обязан стоять ДО общего `handle`.
- `caddy validate` можно прогнать на VM по временному пути, не трогая живой конфиг.
- В контейнере `api` нет пакета `pg` — прод-SQL гонять одноразовым `postgres:17-alpine`
  с `-f файл.sql` (кавычки в `node -e` через ssh не выживают).
- Вывод `console.log` в vitest подавляется — печатать в файл.

## 4а. 🔴 ИНЦИДЕНТ: конвейер не пишет статьи с 31 июля (диагноз готов, фикс НЕ применён)

Вскрыт при пост-деплойной проверке. **К изменениям этой сессии отношения не имеет —
предшествует им на 3 дня.** Владелец решил фикс пока не применять.

**Симптом:** `draft` падает на 100%. По `pipeline_runs`:

| День | draft ok | draft failed |
|---|---|---|
| 28–30.07 | 19–25 | 0–5 |
| 31.07 | **1** | 11 |
| 01–03.08 | **0** | 19 суммарно |

Последняя статья — 31.07, последний пост в канал — **01.08 06:30** (msg 322).
Ingest при этом работает штатно, постинг не на паузе, тихие часы 21→09 МСК.

**Ошибка:** `Agent social / Agent brevity: модель deepseek/deepseek-v4-flash вернула
пустой/усечённый content даже после ретрая (finish_reason=length)`.

**КОРНЕВАЯ ПРИЧИНА (подтверждена поддержкой Timeweb и замером): после перехода
DeepSeek на версию `0731` устаревший параметр `max_tokens` перестал задавать
бюджет вывода** — вместо него применяется дефолтная отсечка 8192, и рассуждения
reasoning-модели упираются в неё, не выдав ни токена контента.

`0731` = 31 июля = ровно дата поломки. Модель, код и длина входа не менялись.

**Фикс: слать `max_completion_tokens`.** Ответ поддержки дословно: «Вам нужно
передавать не max_tokens, а max_completion_tokens. После того как deepseek
перешел на версию 0731 параметр перестал быть актуальным». SDK OpenAI 6.39
согласен: `max_tokens` помечен deprecated в пользу `max_completion_tokens`.

Замер (payload идентичен, отличается ТОЛЬКО имя параметра, по 2 прогона):

| Параметр | finish | completion | reasoning | JSON |
|---|---|---|---|---|
| `max_tokens=10240` | length | **8192** | 8192 | ❌ |
| `max_completion_tokens=10240` | **stop** | 27 671 / 17 628 | 26 679 / 16 539 | ✅ |

⚠️ **Слать ОБА параметра нельзя** — присутствие `max_tokens` возвращает отсечку 8192.

**Сделано:** `packages/agents/src/define-agent.ts` — оба пути (DeepSeek
`json_object` и `tool_choice`) переведены на `max_completion_tokens`; добавлен
регресс-тест `test/max-completion-tokens.test.ts` (в т.ч. «НЕ шлём max_tokens»);
14 ассертов в `agents.test.ts` обновлены. Тесты 63/63, typecheck чист.
**Требует ребилда и `./deploy.sh`** (правка в коде, не в env).

### ⚠️ Мои ошибки в этом расследовании (для протокола)

1. **Неверный первый диагноз.** Замерив 8192 при запросах 10k/18k/32k, я заключил
   «Timeweb срезал кап модели» и уже готовил письмо в поддержку с требованием
   вернуть лимит. Настоящая причина — устаревший ПАРАМЕТР, а не срезанный лимит.
   Урок: одинаковое `completion_tokens` при разных бюджетах значит и «тем ли
   параметром передан бюджет», а не только «отсечка».
2. **Рекомендовал модель, не проверив цену.** `timeweb/qwen3-235b-instruct` —
   1220 ₽/млн против 19/38 ₽/млн у v4-flash (~57× дороже), да ещё и с лимитом
   вывода 4096 (ниже текущего). Владелец завернул справедливо.
3. **Не ответил на главный вопрос владельца** («почему работало два месяца, а
   теперь нет»), прежде чем предлагать смену модели. Ответ был в дате версии.

Прочее, выясненное попутно: `deepseek/deepseek-chat` и `deepseek/deepseek-reasoner`
отдают **HTTP 410 «Model is no longer supported»**, хотя числятся в `/v1/models` —
прежний путь отката на `deepseek-chat` больше не существует. `/v1/models` не
отдаёт ни лимитов, ни тарифов.

## 5. Открытое

- **Пред-существующий баг админки (заведена отдельная задача):** `/posting` и `/cost`
  собираются как `○ Static` и навсегда показывают «Данные недоступны». Главная `/`
  цела (`postponed: true`) — поэтому дефект и не замечали. Фикс — одна строка
  `connection()` на страницу, образец в `apps/admin/src/app/visuals/page.tsx`.
- Перф deep-link (диагностика в хендоффе s29 §2, фикс не применён).
- Защитный фильтр по `articles.status` в `drain-post-slots`.
- Рост диска под обложки: ~4 картинки/день ≈ 60 МБ/мес, очистки нет.
