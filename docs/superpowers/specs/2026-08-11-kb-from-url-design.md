# Спека: база знаний по ссылке

**Дата:** 11 августа 2026 · **вычитана и уточнена по коду 12.08.2026**
**Статус:** ✅ построена и выкачена на прод 12.08.2026 (коммит `85a7bd8`)

> **Что построено.** Миграции `0028`–`0030`; `fetchSafely` + `fetchRaw`
> (`fetch-article.ts`); чистые функции обхода (`site-crawl.ts`) и сам обход
> (`crawl-site.ts`); `KnowledgeExtractAgent` с чисткой выхода; функция Inngest
> `import-knowledge`; маршруты запуска, разбора и приёмки; экран разбора
> `/knowledge/import/[id]` — вариант 1 «приёмка по полкам», выбран владельцем из
> трёх. Тестов 622 (было 538), типизация 10/10, сборка админки с PPR-дырой.
>
> **Выкат 12.08.2026:** миграции `0028`–`0030` применены, re-sync Inngest сделан
> (`{"message":"Successfully registered","modified":true}`), функция
> `x10-pipeline-import-knowledge` видна в списке — их стало 14.
> ⚠️ На живом сайте клиента обход ещё НЕ прогонялся: это прод-запись и расход.
**Связано:** реестр разрыва §3.1 (база знаний), CLAUDE.md §1 (готовность = настройками, без кода)

> **Что изменила вычитка 12.08.** Сверка каждого утверждения с кодом нашла
> четыре расхождения: `fetchArticle` не может прочитать ни `robots.txt`, ни
> `sitemap.xml` (§4); ссылки с главной взять неоткуда (§4); бюджет ВХОДА агента
> не был посчитан (§5); ходу обхода негде лежать (§7). Решения владельца от
> 12.08 — в §4, §5, §6, §7 и §8, помечены датой.

---

## 1. Задача одной фразой

Клиент даёт адрес своего сайта — система обходит несколько страниц, извлекает
тексты и **предлагает** готовые материалы по полкам базы знаний. Человек
утверждает или отклоняет. Печатать час в анкету больше не нужно.

## 2. Почему это первым, а не контент-план или ролики

Замеры на проде 10–11.08.2026:

| Что | Факт |
|---|---|
| Опубликовано статей | 257 |
| Реакций всего | 11 (на 6 статей) |
| **Материалов в базе знаний** | **1** |

Три прогона ручного режима подряд показали одно и то же: при пустой базе агент
пишет про отрасль вообще, потом начинает рекламировать саму компанию, потом
выдумывает конкретику («настройка занимает день»). Три итерации `guidance`
улучшили форму, но не смогли достать факты, которых нет.

**Промпт — не рычаг. Рычаг — вход.** Потолок качества у постов, плана, каруселей
и роликов один и тот же, и поднимает его только наполненная база.

У новой копии клиента база пуста по определению. Значит это не наша внутренняя
недоделка, а первое, обо что спотыкается каждый покупатель — в самый хрупкий
момент, когда он ещё не поверил.

## 3. Что переиспользуется (почти всё)

- 🔴 **`fetchArticle`** (`apps/workers/pipeline/src/lib/fetch-article.ts`) —
  защищённая загрузка страницы: проверка адреса после резолва DNS (включая
  редиректы), запрет приватных диапазонов, потолки времени, объёма и
  переадресаций. Это единственное место, где сервер ходит по адресу из
  пользовательского ввода, и оно уже сделано правильно.
- **`extractText`** — вытягивает текст, отбрасывая скрипты, стили, меню, шапки,
  подвалы и формы; предпочитает `<article>`/`<main>`.
- Схема `kb_shelves` / `kb_documents`: `source` уже допускает `url`, статусы
  заведены через `varchar + CHECK` с прямым расчётом на расширение.
- `loadKnowledge` берёт **только** `status='ready'` — это даёт безопасность по
  умолчанию, см. §6.
- Паттерн агента, Inngest, `guardBilling`, `recordRun`.

**Новых npm-пакетов не требуется.** Библиотеку извлечения текста не берём: то,
что есть, покрывает обычные корпоративные сайты. Вернёмся к вопросу, если на
живых сайтах качество окажется низким — с примерами, а не с догадкой.

## 4. Обход сайта

Вход — один адрес. Дальше:

1. **`robots.txt`**: читаем, уважаем запреты. Это чужой сайт, пусть и клиента.
   Разбираем группу `User-agent: *` и свою `ProAgentAI`. Оттуда же берём
   `Sitemap:`.
2. **Карта страниц**: `sitemap.xml`, если есть. Нет — ссылки с главной, только
   того же хоста.
3. **Отбор до 12 страниц** по адресу и заголовку: приоритет у «о компании»,
   «услуги», «цены», «кейсы», «вопросы», «доставка», «контакты» и латинских
   аналогов. Потолок обязателен: без него один сайт съест бюджет и время.
4. Каждую страницу — через `fetchArticle`. Пауза между запросами: обход не
   должен выглядеть атакой.

Страница без текста (соцсеть, всё рисует скрипт) отсеивается с уже написанным
понятным объяснением — этот текст в `fetchArticle` есть.

### 4.1 🔴 Уточнение 12.08: `fetchArticle` в лоб не годится для шагов 1–2

Вычитка кода нашла две стены, о которые план разбился бы на первом же прогоне:

- **Ни карта сайта, ни `robots.txt` не пройдут.** `fetch-article.ts:199`
  пропускает только `html` и `text/plain`, а `sitemap.xml` приезжает как
  `application/xml` — отказ «по ссылке не страница с текстом». `robots.txt` по
  типу пройдёт, но упрётся в порог `fetch-article.ts:207`: типичный файл в
  60 знаков короче 200 и получит «на странице не нашлось текста».
- **Ссылки с главной взять неоткуда.** `fetchArticle` отдаёт только заголовок и
  текст, сырого HTML наружу нет, а `extractText` вырезает `<nav>`, `<header>`,
  `<footer>` — ровно то меню, где живут ссылки на «услуги» и «цены».

**Решение: один защищённый примитив, две обёртки над ним.** Из `fetchArticle`
выделяется `fetchSafely(url, {accept, maxBytes})` — вся проверка адреса,
ручные редиректы и потолки. Поверх него `fetchArticle` (как есть, поведение не
меняется) и `fetchRaw` для `robots.txt`, `sitemap.xml` и главной страницы.

🔴 Почему именно так, а не второй загрузчик рядом: шапка `fetch-article.ts`
объявляет это место ЕДИНСТВЕННЫМ, где сервер ходит по пользовательскому
адресу. Второй загрузчик сделал бы утверждение ложным, и следующий человек
починил бы защиту в одном месте из двух.

## 5. Агент

`KnowledgeAgent` (`packages/agents/src/agents/knowledge-extract.ts`).

**Вход:** страницы `{url, title, text}` и полки `{slug, title, purpose, question}`.
**Выход:** `{ documents: [{ shelfSlug, title, body, sourceUrl }], notes: [] }`.

Правила в системном промпте:

- Писать **только то, что есть на страницах**. Ничего не досочинять — это то же
  правило, что спасло режим «Пост» после трёх итераций.
- Цены, сроки, гарантии и условия переносить **дословно**. Пересказ цены — это
  новая цена.
- Полке нечего дать — документа для неё не создавать. Пустая полка честнее
  выдуманной.
- Каждый документ несёт адрес страницы-источника.
- `notes` — что осталось непокрытым: «на сайте нет цен», «не нашли кейсов».
  Это и есть подсказка человеку, что дозаполнить руками.

Один прогон на весь обход. Тела документов ограничены тем же потолком, что и в
маршрутах (20 000 знаков).

Уровень модели — `SONNET`, как у `CreationAgent`. На нашем экземпляре
`MODEL_SONNET` и `MODEL_HAIKU` оба ведут в `deepseek-v4-flash`, то есть выбор
сегодня ничего не стоит, а качество извлечения фактов важнее.

### 5.1 🔴 Решение 12.08: бюджет ВХОДА — 6 000 знаков на страницу

Спека ограничивала только выход. Между тем `MAX_TEXT_CHARS = 24 000` на
страницу при потолке в 12 страниц даёт до 288 000 знаков в один промпт —
больше 100 тысяч токенов на вход. Один обход стоил бы дороже суток конвейера и
рисковал не влезть в окно модели.

Берём **6 000 знаков со страницы**, то есть до ~70 000 на весь обход. Обоснование
не в экономии как таковой: «о компании», «услуги» и «цены» выкладывают суть в
первых абзацах, а хвост страницы — это отзывы, повторы и подвал. Платить за
хвост значит платить за воду.

Потолок живёт в коде обхода, а не в `fetchArticle`: тот отдаёт статью человеку
и режет по своей причине.

## 6. Состояние «предложено» — ключевое решение

Найденное **не попадает в базу молча**. Документы создаются со `status='proposed'`,
`source='url'`.

Почему это безопасно по умолчанию: `loadKnowledge` выбирает только `ready`.
Значит непринятое предложение физически не может уехать в промпт агента, даже
если про него забыли. Безопасность держится на уже существующем условии, а не на
новой дисциплине.

Человек принимает документ (`proposed` → `ready`), правит или отклоняет
(удаление строки). Есть «принять все» — иначе кнопка «одна» превращается в
двенадцать.

### 6.1 Решение 12.08: повторный обход сносит прежние непринятые

Нажали «Собрать по ссылке» второй раз — старые `proposed` удаляются, остаются
только свежие. Непринятое предложение ещё не знание, терять там нечего, а вот
две копии одного и того же превращают разбор в сверку.

`ready` не трогаем никогда: принятое человеком — уже его знание, и обход не
вправе его переписывать.

## 7. Миграции

Три, все маленькие, разными файлами:

1. **Статус `proposed`**: `DROP CONSTRAINT` / `ADD CONSTRAINT` на
   `kb_documents_status_chk` со списком `ready · parsing · failed · proposed`.
   Это varchar с CHECK, а не PG-enum — автор 0024 предусмотрел расширение.
2. **Вид расхода `knowledge`** в PG-enum `agent_kind`: `ADD VALUE`, **отдельным
   файлом и без использования в той же транзакции** (CLAUDE.md §8).
3. **Таблица `kb_imports`** — решение 12.08, в первой редакции спеки её не было.

### 7.1 🔴 Почему понадобилась третья: ходу обхода негде лежать

Спека обещает экрану ход обхода, причину отказа и `notes` агента, но места под
них не заводила. Обход — минута-две (до 12 загрузок с паузами плюс прогон
агента), значит Inngest, значит состояние обязано лежать в базе: HTTP-запрос
столько не живёт.

В разделе «Создать» эту роль играет строка `creations` — берём тот же образец:

| Поле | Зачем |
|---|---|
| `site_url` | что обходили |
| `status` | `queued · running · ready · failed` — как у `creations` |
| `status_reason` | человеческая причина отказа: «сайт закрыт от роботов» |
| `pages` (jsonb) | какие адреса взяли, какие отсеяли и почему |
| `notes` (jsonb) | чего на сайте не нашлось — подсказка, что дозаполнить руками |
| `created_by` | кто запустил |

Статусы — `varchar + CHECK`, как в 0024 и 0025: набор ещё будет меняться.

## 8. Экран

Раздел `/knowledge` получает кнопку «Собрать по ссылке». **Решение 12.08:
разбор живёт на ОТДЕЛЬНОМ экране**, а не вперемешку в полках.

Причина в том, что полка отвечает на вопрос «что система знает о моём бизнесе».
Предложенное — это ещё не знание, и его присутствие там сделало бы ответ
неправдой ровно в том месте, где клиент ему верит. Заодно разбор пачкой не
требует обойти семь полок по очереди.

Путь: поле адреса → ход обхода → экран разбора «найдено N материалов»,
сгруппированный по полкам, с «принять», «отклонить» и «принять все». Рядом —
`notes` агента: чего на сайте не нашлось.

Счётчики полок при этом не поедут: `GET /knowledge` уже считает только
`status='ready'`. А вот `GET /knowledge/:slug` отдаёт документы БЕЗ фильтра по
статусу — его нужно ограничить явно, иначе предложенное всплывёт в полке
непомеченным.

Три варианта вёрстки показываю отдельно перед реализацией, по правилу владельца.

## 9. Права и деньги

- Смотреть предложенное и принимать — `catalog.manage`, как и всё в базе знаний.
- `guardBilling` **до** обхода: платить нечем — не ходим по сайту.
- `recordRun` видом `knowledge`, чтобы расход был виден в «Расходах» отдельной
  строкой, а не смешивался с конвейером.

## 10. Тесты (вперёд кода)

Чистые функции — основной предмет проверки:

- отбор страниц: приоритет нужных разделов, потолок 12, только свой хост,
  отсев якорей и файлов;
- разбор `robots.txt`: запреты уважаются, `Sitemap:` находится, группы
  `User-agent: *` и `ProAgentAI` разбираются обе;
- разбор `sitemap.xml`, включая вложенные индексы;
- сбор ссылок с главной из сырого HTML (то, что `extractText` вырезает);
- потолок входа: 6 000 знаков со страницы соблюдён (§5.1);
- промпт агента: правила канона на месте (не выдумывать, цены дословно);
- схема выхода: неизвестный слаг полки не роняет разбор, пустой документ
  отбрасывается.

Маршруты — на права и гейты, как в `admin-create.test.ts`.

🔴 **Гарды проверяем ПОВЕДЕНИЕМ, а не проводкой** (грабля сессии 34). Про
`fetchSafely` это значит: тест даёт адрес, резолвящийся в приватный диапазон, и
ждёт отказ — а не проверяет, что в коде вызвана проверка. Прежние тесты ворот
ревью проверяли строку в SQL и подтверждали вывернутый наизнанку смысл.

⚠️ `pnpm typecheck` гонять ОТДЕЛЬНО от тестов: в замыканиях `step.run` теряется
сужение `let`, а мок без параметров типизирует `mock.calls` пустым кортежем.
Дважды за сессию 34 тесты были зелёными при красной типизации.

## 11. Риски

**Сайт закрыт от роботов или рисуется скриптом.** Честный отказ с объяснением;
тексты уже написаны. Клиенту предлагаем вставить текст руками.

**Клиент вставит чужой адрес.** Технически безвредно, но на экране прямо
сказано: заводим знание о **своём** бизнесе.

**Качество извлечения на сложной вёрстке.** Принято сознательно: начинаем без
новой зависимости, решение пересматриваем по живым примерам.

## 12. Чего в первой версии НЕ делаем

- Выгрузку телеграм-канала клиента как второй источник голоса. Отдельная задача:
  она даёт тон, а не факты, и требует другого доступа.
- Повторный обход по расписанию. Сайт меняется редко, а автоматический обход
  тратил бы деньги без спроса.
- Загрузку файлов (PDF, DOCX). Отдельный путь со своим разбором; статус
  `parsing` под него уже заведён.
