/**
 * Юридические реквизиты и параметры фискального чека.
 *
 * Источник — боевой проект владельца «KORITCA / NanoBanana» (Lovable,
 * `src/lib/consent.ts` и `supabase/functions/create-payment`): реквизиты ИП и
 * связка полей чека там уже прошли через живые платежи ЮKassa.
 *
 * 🔴 Перенесены ТОЛЬКО факты — реквизиты ИП и параметры чека. Тексты оферты и
 * политики оттуда НЕ копировались: они описывают другой сервис (генерация
 * картинок физлицам, Google Gemini, хранение в Supabase, свои тарифы). Дословный
 * перенос дал бы документы, которые врут о том, что мы продаём и куда уходят
 * данные. Наши тексты — `docs/legal/`.
 *
 * Единый источник: отсюда параметры берёт и чек в платеже, и страницы с
 * реквизитами. Разъехаться они не должны.
 */

/** Реквизиты продавца. Публичны по закону — печатаются в чеке и в оферте. */
export const MERCHANT = {
  legalName: "ИП Сыров Константин Александрович",
  shortName: "ИП Сыров К.А.",
  inn: "910921843083",
  ogrnip: "325911200128086",
  /** Адрес регистрации по ЕГРИП. */
  registrationAddress:
    "Республика Крым, м.р-н Белогорский, с.п. Ароматновское, с. Ароматное",
  /** Почтовый адрес для претензий и обращений. */
  postalAddress:
    "297635, Республика Крым, Белогорский район, с. Ароматное, ул. Виноградная, д. 28",
  phone: "+7 (978) 585-06-50",
  email: "syrovkostia@gmail.com",
  /** УСН — отсюда «НДС не облагается» в чеке и в оферте (ст. 346.11 НК РФ). */
  taxSystem: "УСН (упрощённая система налогообложения)",
  /** Коды ОКВЭД: первый — основной. */
  okved: ["62.01", "62.02", "62.09", "63.11", "63.11.1"],
} as const;

/**
 * Параметры позиции фискального чека ЮKassa (54-ФЗ).
 *
 * Значения сняты с боевого кода «Корицы», где чеки уже пробиваются облачной
 * кассой ЮKassa:
 *
 * - `vatCode: 1` — «без НДС». Соответствует УСН продавца.
 * - `paymentSubject: "service"` — предмет расчёта «услуга».
 * - `paymentMode: "full_payment"` — полный расчёт: клиент платит вперёд и
 *   сразу получает пополнение баланса.
 *
 * ⚠️ `tax_system_code` в запрос НЕ передаётся намеренно: у магазина в ЛК
 * ЮKassa настроена одна система налогообложения, и тогда поле необязательно.
 * Появится второй режим — придётся передавать явно.
 *
 * ⚠️ `customer.email` обязателен: без него касса не выбьет чек. Значит перед
 * оплатой email надо спросить и сохранить.
 */
export const RECEIPT = {
  vatCode: 1,
  paymentSubject: "service",
  paymentMode: "full_payment",
  /**
   * Наименование услуги в чеке — это увидит клиент. Формулировка описывает
   * то, что он реально покупает: не «токены» (клиенту это ничего не говорит и
   * выглядит как перепродажа чужой услуги), а работу завода.
   */
  itemDescription: "Услуги по автоматической подготовке контента",
} as const;

/** Строка про НДС для оферты и чека — одна на весь проект. */
export const VAT_NOTE =
  "НДС не облагается в связи с применением Исполнителем упрощённой системы " +
  "налогообложения (УСН) на основании ст. 346.11 НК РФ.";
