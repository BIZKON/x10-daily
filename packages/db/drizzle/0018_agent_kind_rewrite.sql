-- Новый агент конвейера: rewrite (Спека 4, шаг 5) — переписывает материал по
-- правке редактора из Telegram.
--
-- Значение нужно в `agent_kind`, чтобы расход рерайта считался ОТДЕЛЬНОЙ
-- строкой в $-ledger. Спрятать его под чужой агент (например `tov`) значило бы
-- соврать в отчёте о расходах — а он поедет клиенту как счёт.
--
-- ⚠️ ADD VALUE отдельным файлом и БЕЗ использования нового значения здесь
-- (CLAUDE.md §8): PG запрещает применять только что добавленное значение в той
-- же транзакции. Код начнёт его писать уже после коммита этой миграции.
ALTER TYPE "agent_kind" ADD VALUE IF NOT EXISTS 'rewrite';
