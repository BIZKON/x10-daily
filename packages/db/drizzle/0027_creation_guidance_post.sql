-- Режим «Пост»: правила письма после двух живых прогонов (10.08.2026).
--
-- Зачем миграция, а не правка руками в базе. `guidance` — конфигурация
-- экземпляра, и её текст обязан приезжать в НОВУЮ копию клиента вместе с
-- остальной поставкой. Поправленный только у нас в проде, он превратил бы
-- «работает у нас» в «у клиента по-старому» — ровно тот разрыв, который
-- CLAUDE.md §1 объявляет признаком неготовности.
--
-- Что показали прогоны (обе темы одинаковые, менялся только этот текст):
--
--  1. Со стартовым текстом модель уводила середину материала в рассказ о самом
--     клиенте: тема была про сверку актов, а второй абзац — про то, чем
--     занимается заказчик. База знаний работала как СОДЕРЖАНИЕ вместо
--     КОНТЕКСТА. Отсюда явный запрет пересказывать бизнес клиента.
--
--  2. 🔴 Требование конкретики заставило модель конкретику ВЫДУМЫВАТЬ. Убрав
--     расплывчатое «в разы», она написала «настройка занимает день» — срок,
--     которого нет ни в задании, ни в сведениях. Это опаснее расплывчатости:
--     расплывчатость никого не обманет, а срок внедрения клиент прочитает как
--     обещание. Правило «цифры только из задания» модель к срокам НЕ применила
--     — «день» цифрой не сочла. Поэтому запрет расширен со «цифр» на любые
--     конкретные утверждения о сроках, стоимости, объёме и совместимости.
--
--  3. Абсолютные обещания вернулись переформулированными: вместо запрещённого
--     «ошибки исключаются» появилось «ошибки перестают попадать в документы».
--     Запрет по смыслу модель обходит, поэтому ниже он задан примерами.
--
-- Идемпотентно: UPDATE по slug. Повторный прогон миграции безвреден.
UPDATE "creation_modes" SET
  "guidance" = $g$Напиши пост для канала клиента.

ОБЪЁМ: 250–300 слов, чтение за полминуты.

ПЕРВАЯ ФРАЗА: число или конкретный проверяемый факт по теме. Не сценка, не общее вступление, не вопрос читателю.

О ЧЁМ ПИШЕШЬ: ровно о теме задания. Сведения о клиенте — это КОНТЕКСТ для точности, а НЕ материал для текста. Не пересказывай, чем занимается клиент, не описывай его продукт и не рекламируй его, если тема прямо не об этом. Текст, который начинается за тему, а заканчивается рассказом о компании, — реклама, а не польза.

🔴 НЕ ВЫДУМЫВАЙ КОНКРЕТИКУ. Это правило шире, чем про цифры. Сроки внедрения, длительность настройки, стоимость, объём работ, совместимость с чужими системами, гарантии и окупаемость — конкретика. Если её нет в задании или в сведениях о клиенте, о ней не пишут вовсе. Запрещено, даже если звучит правдоподобно: «настройка занимает день», «не требует замены учётной системы», «окупается за три месяца», «внедрение за неделю». Не хватает факта — скажи об этом в notes, а не подставляй правдоподобное.

ЦИФРЫ: только из задания и из сведений о клиенте. Нет цифры — не подставляй вместо неё расплывчатое. Запрещены «в разы», «кратно», «значительно», «несколько часов» и подобные: либо конкретная величина, либо честная фраза без числа.

ОБЕЩАНИЯ: без абсолютных — ни прямо, ни в пересказе. Запрещены «исключаются», «уходит в прошлое», «перестают», «полностью», «всегда», «никогда», «гарантированно». Пиши «становится меньше», «реже», «часть работы берёт на себя».

ПОЛЬЗА: обязательно скажи, что это даёт бизнесу читателя — часы, деньги или конверсию. Если конверсия не при чём, не притягивай её.

ЕСЛИ ТЕМА НЕ ПОКРЫТА сведениями о клиенте: пиши по теме общими знаниями и скажи об этом в notes. Подменять недостающее рассказом о клиенте нельзя.

БЕЗ хайпа и инфобиз-лексики.$g$,
  "updated_at" = now()
WHERE "slug" = 'post';
