-- Сборка контент-плана — отдельный вид прогона.
--
-- Экран «Расходы» группирует затраты ПО ВИДУ АГЕНТА. Разовая сборка плана на
-- месяц и ежедневная работа сотрудника — разные строки в счёте клиента, и
-- смешивать их значит потерять возможность объяснить счёт.
--
-- 🔴 ОТДЕЛЬНЫЙ ФАЙЛ. PostgreSQL запрещает использовать новое значение enum в
-- транзакции, где оно добавлено (CLAUDE.md §8).
ALTER TYPE "agent_kind" ADD VALUE IF NOT EXISTS 'plan';
