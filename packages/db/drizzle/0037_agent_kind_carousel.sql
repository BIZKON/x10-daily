-- Вид агента «карусель» в журнале прогонов.
--
-- 🔴 ОТДЕЛЬНЫМ файлом от 0036 намеренно: PostgreSQL запрещает использовать
-- новое значение enum в той же транзакции, в которой оно добавлено. Мы его
-- здесь и не используем — только объявляем; писать строки с ним начнёт
-- воркер, уже после того как эта миграция закоммитится.
--
-- Отдельный вид, а не `visual`, по той же причине, что `creation` и
-- `knowledge`: экран «Расходы» группирует по виду агента, и «нарисовали
-- карусель» против «нарисовали обложку» — разные строки в счёте клиента.
ALTER TYPE "agent_kind" ADD VALUE IF NOT EXISTS 'carousel';
