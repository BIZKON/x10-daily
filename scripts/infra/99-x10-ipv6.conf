# x10-daily: IPv6 для docker-контейнеров (api.telegram.org доступен только по IPv6 в РФ).
# accept_ra=2 ПЕРЕД forwarding — иначе forwarding сносит RA-маршрут eth0.
net.ipv6.conf.eth0.accept_ra=2
net.ipv6.conf.all.forwarding=1
